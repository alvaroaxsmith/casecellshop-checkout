{
  "name": "casecellshop-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Testes end-to-end (Playwright) do fluxo de checkout do CaseCellShop, cobrindo frontend + backend + erp-mock juntos.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
